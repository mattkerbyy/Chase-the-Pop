import React from 'react'
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Returns and Refunds",
  description:
    "Find out how our returns and refund process works—including eligibility, timelines, cost, and guarantee policies—so you can shop with confidence.",
};

function Returns() {
  return (
    <div>Returns</div>
  )
}

export default Returns